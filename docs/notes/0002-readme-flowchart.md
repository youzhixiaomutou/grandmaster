---
id: 0002-readme-flowchart
title: README 运转流程图（Mermaid，双语）
created: 2026-07-31
tags: [doc, readme, diagram]
---

## 摘要

`README.md` / `README.zh-CN.md` 的「研发流水线」段改为 **Mermaid 流程图 + 阅读指南**，展示使用 Grandmaster 的项目运转：需求 → `requirement-intake`（澄清 + 固化）→ `design-proposal`（**用户确认门**）→ 实现（+ `testing` 全绿）→ `version-control`（PR）→ `documentation`；`redlines` CI（密钥 / 结构 / process-gate）；`memory` 横切（recall / save）；每步 **fan-out** 固化 `local` + `github` / `gitlab`。

## 链接

- `README.md` / `README.zh-CN.md` 的「研发流水线 —— 使用 Grandmaster 的项目如何运转」段
